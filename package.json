{
  "name": "goodluck-pocket-v3",
  "version": "4.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.114.0"
  }
}
